apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml
  - ingress.yaml

nameSuffix: -prod

replicas:
  - name: laptopzone-backend
    count: 2
  - name: laptopzone-frontend
    count: 2
