apiVersion: v1
kind: Service
metadata:
  name: laptopzone-frontend
  labels:
    app: laptopzone-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: laptopzone-frontend
